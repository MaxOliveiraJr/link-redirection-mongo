<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>

    <h1>Editar Link</h1>
    <form action="/edit/<%=body.id%>" method="post">
        <input type="text" name="title" required placeholder="Título" value="<%=body.title%>">
        <input type="text" name="description" placeholder="Descrição" value="<%=body.description%>">
        <input type="url" name="url" required placeholder="https://exemplo.com" value="<%=body.url%>">

        <button type="submit">Salvar</button>

    </form>

    <%if(error){%>
        <h3>
            <%=error.message%>
        </h3>
        <%}%>

</body>

</html>